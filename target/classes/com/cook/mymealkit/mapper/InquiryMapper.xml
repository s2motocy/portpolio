<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
	<mapper namespace="com.cook.mymealkit.mapper.InquiryMapper">
	
	<insert id="InquiryInsert" parameterType="com.cook.mymealkit.domain.InquiryVO">
		insert into tbl_inquiry(qno, title, content, wrtier, register_date, update_date, reply_cnt)
		values(seq_Inquiry.nextval,#{title},#{content},#{wrtier},#{register_date},#{update_date},#{reply_cnt})
	</insert>
	
	<select id="quest" resultType="com.cook.mymealkit.domain.InquiryVO">
		select * from tbl_inquiry
	</select>
	
	</mapper>